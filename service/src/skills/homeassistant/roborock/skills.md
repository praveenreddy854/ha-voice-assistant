# Roborock Integration Skill

Source: https://www.home-assistant.io/integrations/roborock/

## Entities
- `vacuum.<name>`
- `camera.<name>`
- `image.<name>`
- `number.<name>`
- `select.<name>`
- `sensor.<name>`
- `switch.<name>`

## Actions
- `roborock.get_vacuum_current_position`
- `roborock.set_vacuum_goto_position` (requires `x` and `y` coordinates)

## Notes
- Available entities and actions depend on the model and account setup.
